# Pinned exactly. CPU-only wheels: the deployment hosts have no GPU.
# keras is pinned explicitly because models/final_fc_model.keras was saved
# with Keras 3.13.2 — an older Keras cannot read that archive.
tensorflow-cpu==2.21.0
keras==3.15.1
numpy==2.3.2
fastapi==0.128.0
uvicorn==0.40.0
pydantic==2.12.5
python-multipart==0.0.32
